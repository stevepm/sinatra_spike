

<html>
  <body>
    <a href="new">New</a>
    <% menu = ItemList.new %>
    <% menu.add_item(@name) %>
    <ul>

      <% menu.list_items(@search.to_s).each do |item|%>
        <li><a href="item1"><%= item.name %></a></li>
      <% end %>
    </ul>
  </body>
</html>